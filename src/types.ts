export interface CarData {
  name: string,
  color: string
}

export interface Car {
  name: string,
  color: string,
  id: number
}

export interface Winner {
  'id': number,
  'wins': number,
  'time': number
}

export interface WinnerData {
  'wins': number,
  'time': number
}
